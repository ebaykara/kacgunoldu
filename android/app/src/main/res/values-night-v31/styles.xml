<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is on -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:windowSplashScreenBackground">@color/launch_kiremit</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon_blank</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.
         
         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- FlutterActivity switches to this theme mid-launch. Theme.Light draws
             no system bar backgrounds, so the navigation bar went black under
             the launch view; draw them, see-through, and the view shows under
             them. The app sets its own bar colours once it is on screen. -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/launch_kiremit</item>
        <!-- Without this, a phone in dark mode darkens the window while the
             Flutter engine starts (the grey/black screen before the splash). -->
        <item name="android:forceDarkAllowed">false</item>
    </style>

    <!-- One launch theme per colour theme. The app tells Android which to use
         for the next launch (MainActivity, Android 13+); the hex values mirror
         Palette.launchColor in lib/theme/tokens.dart. -->
    <style name="LaunchTheme.Kiremit" parent="LaunchTheme">
        <item name="android:windowSplashScreenBackground">@color/launch_kiremit</item>
    </style>
    <style name="LaunchTheme.Okyanus" parent="LaunchTheme">
        <item name="android:windowSplashScreenBackground">@color/launch_okyanus</item>
    </style>
    <style name="LaunchTheme.Orman" parent="LaunchTheme">
        <item name="android:windowSplashScreenBackground">@color/launch_orman</item>
    </style>
    <style name="LaunchTheme.Lavanta" parent="LaunchTheme">
        <item name="android:windowSplashScreenBackground">@color/launch_lavanta</item>
    </style>
    <style name="LaunchTheme.Gul" parent="LaunchTheme">
        <item name="android:windowSplashScreenBackground">@color/launch_gul</item>
    </style>
    <style name="LaunchTheme.Gece" parent="LaunchTheme">
        <item name="android:windowSplashScreenBackground">@color/launch_gece</item>
    </style>
</resources>

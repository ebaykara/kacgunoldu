<?xml version="1.0" encoding="utf-8"?>
<!-- One card in the "Kartlar" widget: the app's list row (card_row_tile). -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/row_root"
    android:layout_width="match_parent"
    android:layout_height="56dp"
    android:layout_marginTop="6dp">

    <ImageView
        android:id="@+id/row_bg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:importantForAccessibility="no"
        android:scaleType="fitXY"
        android:src="@drawable/widget_row_bg" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="10dp"
        android:paddingEnd="10dp">

        <FrameLayout
            android:layout_width="36dp"
            android:layout_height="36dp">

            <ImageView
                android:id="@+id/row_ring"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:importantForAccessibility="no" />

            <ImageView
                android:id="@+id/row_glyph"
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:layout_gravity="center"
                android:importantForAccessibility="no" />
        </FrameLayout>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:layout_marginEnd="8dp"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/row_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:fontFamily="@font/archivo_semibold"
                android:maxLines="1"
                android:textSize="14sp" />

            <TextView
                android:id="@+id/row_status"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="1dp"
                android:ellipsize="end"
                android:fontFamily="@font/archivo_medium"
                android:maxLines="1"
                android:textSize="12sp" />

            <TextView
                android:id="@+id/row_status_bold"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="1dp"
                android:ellipsize="end"
                android:fontFamily="@font/archivo_bold"
                android:maxLines="1"
                android:textSize="12sp"
                android:visibility="gone" />
        </LinearLayout>

        <!-- The count: right-aligned in a fixed column, "gün oldu" under it. -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="end"
            android:minWidth="52dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/row_days"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="@font/instrument_serif"
                android:includeFontPadding="false"
                android:maxLines="1"
                android:textSize="30sp" />

            <TextView
                android:id="@+id/row_unit"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="@font/archivo_medium"
                android:maxLines="1"
                android:textSize="10sp" />
        </LinearLayout>

        <!-- "Bugün yaptım" (when on in the settings). -->
        <FrameLayout
            android:id="@+id/row_done"
            android:layout_width="34dp"
            android:layout_height="34dp"
            android:layout_marginStart="10dp"
            android:visibility="gone">

            <ImageView
                android:id="@+id/row_done_bg"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:importantForAccessibility="no"
                android:scaleType="fitXY"
                android:src="@drawable/widget_pill_bg" />

            <ImageView
                android:id="@+id/row_done_icon"
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:layout_gravity="center"
                android:importantForAccessibility="no"
                android:src="@drawable/wg_check" />
        </FrameLayout>
    </LinearLayout>
</FrameLayout>
